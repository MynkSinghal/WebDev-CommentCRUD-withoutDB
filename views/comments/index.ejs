<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Comments Index</title>
    <style>
        body {
            background-color: #1a1a1a;
            color: #f0a500;
            font-family: 'Arial', sans-serif;
        }
        .header {
            font-size: 2em;
            margin-bottom: 20px;
        }
        .comments-list {
            list-style-type: none;
            padding: 0;
        }
        .comment-item {
            background-color: #333;
            padding: 15px;
            margin-bottom: 10px;
            border-radius: 5px;
        }
        .comment-text {
            display: block;
            margin-bottom: 10px;
        }
        .button {
            background-color: #f0a500;
            color: #1a1a1a;
            border: none;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1em;
        }
        .button:hover {
            background-color: #ffba26;
        }
    </style>
</head>

<body>
    <h1 class="header">Comments</h1>
    <ul class="comments-list">
        <% for(let c of comments) {%>
        <li class="comment-item">
            <span class="comment-text"><%=c.comment%> - <b class="comment-username"><%=c.username%></b></span>
            <a href="/comments/<%= c.id %>" class="button">Details</a>
        </li>
        <% }%>
    </ul>
    <a href="/comments/new" class="button">New Comment</a>
</body>

</html>
